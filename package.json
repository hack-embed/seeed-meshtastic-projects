{
  "name": "mesh-lab",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 -m http.server 8765 --bind 127.0.0.1 --directory docs"
  },
  "devDependencies": { "@playwright/test": "^1.58.0" }
}
